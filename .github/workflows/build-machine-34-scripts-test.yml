name: Build Machine 34 Scripts Test

on:
  workflow_dispatch:

  push:
    branches:
      - ci/machine-34-scripts
    paths:
      - "34号机_线上环境_qtmm1/*.py"
      - ".github/workflows/build-machine-34-scripts-test.yml"

permissions:
  contents: read

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Install uv and Python
        uses: astral-sh/setup-uv@v8.3.2
        with:
          python-version: "3.12"
          enable-cache: true

      - name: Install build dependencies
        shell: pwsh
        run: |
          uv venv
          uv pip install pyinstaller requests selenium

      - name: Check all scripts
        shell: pwsh
        run: |
          $scripts = Get-ChildItem "34号机_线上环境_qtmm1" -Filter *.py
          if ($scripts.Count -eq 0) {
            throw "34号机_线上环境_qtmm1 目录中没有找到 Python 文件"
          }
          foreach ($script in $scripts) {
            uv run --no-sync python -m py_compile $script.FullName
            if ($LASTEXITCODE -ne 0) {
              throw "语法检查失败: $($script.Name)"
            }
          }

      - name: Build every script as a standalone EXE
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          $outputDir = "package\34号机脚本"
          New-Item -ItemType Directory -Path $outputDir -Force | Out-Null

          Get-ChildItem "34号机_线上环境_qtmm1" -Filter *.py | ForEach-Object {
            $name = $_.BaseName
            uv run --no-sync pyinstaller `
              --clean `
              --onefile `
              --console `
              --collect-all selenium `
              --name $name `
              $_.FullName

            if ($LASTEXITCODE -ne 0) {
              throw "打包失败: $($_.Name)"
            }

            Copy-Item "dist\$name.exe" $outputDir
          }

      - name: Upload Machine 34 package
        uses: actions/upload-artifact@v7
        with:
          name: 34号机脚本
          path: package/34号机脚本/
          if-no-files-found: error
          retention-days: 30
