[project]
name = "rpa-refactor"
version = "0.1.0"
description = "Async Playwright engine for resilient BitBrowser backfill automation"
requires-python = ">=3.12"
dependencies = [
    "playwright>=1.60.0",
    "pyinstaller>=6.21.0",
    "python-dotenv>=1.1.1",
    "requests>=2.34.2",
]
