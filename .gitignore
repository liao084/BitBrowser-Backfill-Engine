# ==============================================================================
# Strict Gitignore: 只允许项目源码、文档和可复现依赖配置上传
# ==============================================================================

# 1. 默认忽略当前目录下的一切文件和文件夹
*

# 2. 将需要在多端（公司/家里）同步的源码、文档和依赖配置加入白名单
!*.py
!*.md
!.gitignore
!.env.example
!notify_agent.env.example
!pyproject.toml
!uv.lock
!.python-version

# 3. 补丁黑名单：坚决屏蔽运行时的产物（哪怕它们刚好是 py 或 md 后缀）
# PyInstaller 打包产物
build/
dist/
temp/
*.exe
*.spec

# 日志文件 (避免公司和家里的日志冲突)
*.log
*.jsonl

# 真实部署配置不进入仓库；只提交脱敏模板 .env.example
.env
.env.*
!.env.example

# 虚拟环境与缓存
.venv/
venv/
__pycache__/
*.pyc

# 操作系统自动生成文件
.DS_Store
Thumbs.db

# 主管原脚本仅用于本地设计参考，不属于本项目交付内容
131_pdd_lpjy.py
